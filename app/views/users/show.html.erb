

<h1>Profile</h1>

<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div>
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this change from being saved:</h2>

      <ul>
      <% @user.each do |m| %>
        <li><%= name %></li>
        <li><%= email %></li>
        <li><%= password %></li>
        <li><%= skill %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :name %><br>
    <%= f.text_field :name %><br>
    <%= f.label :email %><br>
    <%= f.text_field :email %><br>
  </div>
  <div>
    <%= f.button do %>
       Update
    <% end %>
  </div>
<% end %>
